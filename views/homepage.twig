{% extends 'layout.twig' %}

{% block content %}
<div class="button-list">
    <ul class="action-buttons-list">
        <li>
            <a href="{{ path('programmer_new') }}" class="btn btn-lg btn-default">Create Programmer</a>
        </li>
        <li>
            <a href="{{ path('programmer_choose') }}" class="btn btn-lg btn-danger">Start Battle</a>
        </li>
        <li>
            <a href="{{ path('battle_list') }}" class="btn btn-lg btn-info">Scores</a>
        </li>
    </ul>
</div>
{% endblock %}
